sudo apt update

sudo apt install python3 (esto no hará falta)

sudo apt install pip

pip3 install flask --break-system-packages

Tendremos que crear una aplicación mínima flask de tipo hola mundo

sudo apt install gedit
Primero importo todas las librerías que necesite
stdio = standard input and output

#include <stdio.h>

Tiene que haber un punto de entrada de la ejecucion - obligatorio
Y la funcion de entrada se tiene que llamar main

C es un lenguaje fuertemente tipificado
int = devuelve un numero entero

int main(){
	printf("Hola mundo desde C \n");
  return 1;
}

Funcion de impresión/salida por consola
printf("Hola mundo desde C \n");

Si te has comprometido a devolver un int, tienes que devolver un int
return 1;